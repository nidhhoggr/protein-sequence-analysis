services:
  ebolavirus-analysis:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ebolavirus-analysis
    volumes:
      # Mount local directories into container
      - ./sequences:/data/sequences
      - ./results:/data/results
      - ./scripts:/data/scripts
      - ./config:/data/config
    working_dir: /data
    stdin_open: true
    tty: true
    environment:
      - PYTHONUNBUFFERED=1

    # Example: uncomment to run pipeline automatically
    # command: python scripts/pipeline.py -i sequences/ebolavirus_gp_combined.fasta -o results -n 40 -c config/species_config.json
